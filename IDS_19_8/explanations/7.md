# Question 7: Traverse Through 'n' Elements in an Array and Print Each

## Problem Statement
Write a program to traverse throw n elements in an array and print each.

---

## Complete Code (`7.cpp`)
```cpp
// 7. write a program to traverse throw n elements in an array and print each

#include <iostream>
using namespace std;

int main() {
    int n;
    cout << "Enter number of elements: ";
    cin >> n;

    int arr[n];
    cout << "Enter " << n << " elements:" << endl;
    for (int i = 0; i < n; i++) {
        cin >> arr[i];
    }

    cout << "Traversing array elements:" << endl;
    for (int i = 0; i < n; i++) {
        cout << "Element at index " << i << ": " << arr[i] << endl;
    }

    return 0;
}
```

---

## Line-by-Line Explanation

- **Line 1:** `// 7. write a program to traverse throw n elements in an array and print each`  
  Title comment.

- **Line 3:** `#include <iostream>`  
  Enables console input and output.

- **Line 4:** `using namespace std;`  
  Provides convenient access to standard library symbols.

- **Line 6:** `int main() {`  
  Main function definition.

- **Line 7:** `int n;`  
  Stores the total number of elements.

- **Line 8-9:**  
  ```cpp
  cout << "Enter number of elements: ";
  cin >> n;
  ```  
  Prompts and takes input for the array size $n$.

- **Line 11:** `int arr[n];`  
  Allocates array of size $n$.

- **Line 12-15:**  
  ```cpp
  cout << "Enter " << n << " elements:" << endl;
  for (int i = 0; i < n; i++) {
      cin >> arr[i];
  }
  ```  
  Loop that populates the array with user values.

- **Line 17:** `cout << "Traversing array elements:" << endl;`  
  Prints a header indicating that traversal is starting.

- **Line 18-20:**  
  ```cpp
  for (int i = 0; i < n; i++) {
      cout << "Element at index " << i << ": " << arr[i] << endl;
  }
  ```  
  **Traversal:** The loop visits every index sequentially from `0` to `n - 1`, accessing and printing the value at `arr[i]` along with its index.

- **Line 22:** `return 0;`  
  Exits the program successfully.

- **Line 23:** `}`  
  Ends `main()`.

---

## Example Walkthrough
- **Input:**  
  `n = 3`  
  `Elements: 10 20 30`
- **Output:**  
  `Traversing array elements:`  
  `Element at index 0: 10`  
  `Element at index 1: 20`  
  `Element at index 2: 30`
