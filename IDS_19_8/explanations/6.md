# Question 6: Read Two Arrays and Check if Both are Equal

## Problem Statement
Write a program to read two array of integers and check both are equal or not.

---

## Complete Code (`6.cpp`)
```cpp
// 6. write a program to read two array of integers and check both are equal or not

#include <iostream>
using namespace std;

int main() {
    int n1, n2;

    cout << "Enter size of first array: ";
    cin >> n1;
    int arr1[n1];
    cout << "Enter elements of first array:" << endl;
    for (int i = 0; i < n1; i++) {
        cin >> arr1[i];
    }

    cout << "Enter size of second array: ";
    cin >> n2;
    int arr2[n2];
    cout << "Enter elements of second array:" << endl;
    for (int i = 0; i < n2; i++) {
        cin >> arr2[i];
    }

    if (n1 != n2) {
        cout << "Arrays are NOT equal (different size)." << endl;
        return 0;
    }

    bool equal = true;
    for (int i = 0; i < n1; i++) {
        if (arr1[i] != arr2[i]) {
            equal = false;
            break;
        }
    }

    if (equal) {
        cout << "Both arrays are equal." << endl;
    } else {
        cout << "Arrays are NOT equal." << endl;
    }

    return 0;
}
```

---

## Line-by-Line Explanation

- **Line 1:** `// 6. write a program to read two array of integers and check both are equal or not`  
  Comment stating the program's intent.

- **Line 3:** `#include <iostream>`  
  Preprocessor inclusion for stream input/output.

- **Line 4:** `using namespace std;`  
  Enables standard names without prefix.

- **Line 6:** `int main() {`  
  Program execution start.

- **Line 7:** `int n1, n2;`  
  Declares sizes for the first and second array respectively.

- **Line 9-15:**  
  ```cpp
  cout << "Enter size of first array: ";
  cin >> n1;
  int arr1[n1];
  cout << "Enter elements of first array:" << endl;
  for (int i = 0; i < n1; i++) {
      cin >> arr1[i];
  }
  ```  
  Prompts user for size `n1`, creates array `arr1` of size `n1`, and reads its elements.

- **Line 17-23:**  
  ```cpp
  cout << "Enter size of second array: ";
  cin >> n2;
  int arr2[n2];
  cout << "Enter elements of second array:" << endl;
  for (int i = 0; i < n2; i++) {
      cin >> arr2[i];
  }
  ```  
  Prompts user for size `n2`, creates array `arr2` of size `n2`, and reads its elements.

- **Line 25-28:**  
  ```cpp
  if (n1 != n2) {
      cout << "Arrays are NOT equal (different size)." << endl;
      return 0;
  }
  ```  
  If the sizes differ, the arrays can never be identical. The program prints the message and exits immediately.

- **Line 30:** `bool equal = true;`  
  A boolean flag initialized to `true`, assuming both arrays are equal until proven otherwise.

- **Line 31:** `for (int i = 0; i < n1; i++) {`  
  Loops through each index from 0 to $n1 - 1$.

- **Line 32-35:**  
  ```cpp
  if (arr1[i] != arr2[i]) {
      equal = false;
      break;
  }
  ```  
  If elements at index `i` differ, set flag `equal = false` and `break` out of the loop immediately (no need to check further).

- **Line 36:** `}`  
  Closes checking loop.

- **Line 38-42:**  
  ```cpp
  if (equal) {
      cout << "Both arrays are equal." << endl;
  } else {
      cout << "Arrays are NOT equal." << endl;
  }
  ```  
  If `equal` is still `true`, prints that arrays are equal; otherwise prints they are not equal.

- **Line 44:** `return 0;`  
  Program exit.

- **Line 45:** `}`  
  Closes `main()`.

---

## Example Walkthrough
- **Case 1 (Equal):**  
  Arr 1: `[1, 2, 3]`  
  Arr 2: `[1, 2, 3]`  
  `Both arrays are equal.`
- **Case 2 (Unequal elements):**  
  Arr 1: `[1, 2, 3]`  
  Arr 2: `[1, 5, 3]`  
  At index 1: $2 \ne 5 \rightarrow equal = false \rightarrow$ `Arrays are NOT equal.`
