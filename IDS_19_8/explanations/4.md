# Question 4: Find Average of N Numbers

## Problem Statement
Write a program to find average of N numbers.

---

## Complete Code (`4.cpp`)
```cpp
// 4. write a program to find average of N numbers

#include <iostream>
using namespace std;

int main() {
    int n;
    cout << "Enter the total count of numbers (N): ";
    cin >> n;

    int arr[n];
    cout << "Enter " << n << " numbers:" << endl;
    
    for(int i = 0; i < n; i++){
        cin >> arr[i];
    }   
                 
    int sum = 0;
    for (int i = 0; i < n; i++) {
        sum += arr[i];
    }

    int average = sum / n;
    cout << "Average of the numbers: " << average << endl;

    return 0;
}
```

---

## Line-by-Line Explanation

- **Line 1:** `// 4. write a program to find average of N numbers`  
  Program objective comment.

- **Line 3:** `#include <iostream>`  
  Standard input/output stream header.

- **Line 4:** `using namespace std;`  
  Enables direct access to standard identifiers like `cin`, `cout`.

- **Line 6:** `int main() {`  
  Main entry point function.

- **Line 7:** `int n;`  
  Declares variable `n` to store the total count of numbers.

- **Line 8:** `cout << "Enter the total count of numbers (N): ";`  
  Prints prompt for the count $N$.

- **Line 9:** `cin >> n;`  
  Reads the value of $N$.

- **Line 11:** `int arr[n];`  
  Creates an array of size $N$ to store the inputs.

- **Line 12:** `cout << "Enter " << n << " numbers:" << endl;`  
  Prompts user to input the numbers.

- **Line 14-16:**  
  ```cpp
  for(int i = 0; i < n; i++){
      cin >> arr[i];
  }
  ```  
  Reads all $N$ numbers from user input and stores them in array indices $0$ to $n-1$.

- **Line 18:** `int sum = 0;`  
  Initializes accumulator variable `sum` to 0.

- **Line 19-21:**  
  ```cpp
  for (int i = 0; i < n; i++) {
      sum += arr[i];
  }
  ```  
  Iterates through the array and adds each element to `sum` (`sum = sum + arr[i]`).

- **Line 23:** `int average = sum / n;`  
  Calculates average by dividing `sum` by total elements `n`.

- **Line 24:** `cout << "Average of the numbers: " << average << endl;`  
  Displays the computed average.

- **Line 26:** `return 0;`  
  Signals that program executed without errors.

- **Line 27:** `}`  
  Closes `main()`.

---

## Example Walkthrough
- **Input:**  
  `N = 4`  
  `Numbers: 10 20 30 40`
- `sum = 10 + 20 + 30 + 40 = 100`
- `average = 100 / 4 = 25`
- **Output:** `Average of the numbers: 25`
