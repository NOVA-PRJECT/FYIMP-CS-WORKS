# Question 17: Check if a String is a Palindrome

## Problem Statement
Program to check a string is palindrome or not.

---

## Complete Code (`17.cpp`)
```cpp
// 17. program to check a string is palindrome or not

#include <iostream>
#include <string>
using namespace std;

int main() {
    string str;

    cout << "Enter a string: ";
    cin >> str;

    int left = 0;
    int right = str.length() - 1;
    bool isPalindrome = true;

    while (left < right) {
        if (str[left] != str[right]) {
            isPalindrome = false;
            break;
        }
        left++;
        right--;
    }

    if (isPalindrome) {
        cout << str << " is a palindrome." << endl;
    } else {
        cout << str << " is not a palindrome." << endl;
    }

    return 0;
}
```

---

## Line-by-Line Explanation

- **Line 1:** Title comment.

- **Line 3-4:** Includes `<iostream>` and `<string>`.

- **Line 5:** `using namespace std;`

- **Line 7:** `int main() {`  
  Execution starts.

- **Line 8:** `string str;`  
  Declares variable `str` to hold the word entered by the user.

- **Line 10-11:**  
  Prompts and takes input of the string into `str`.

- **Line 13:** `int left = 0;`  
  Initializes a left pointer pointing to the first character of the string (index 0).

- **Line 14:** `int right = str.length() - 1;`  
  Initializes a right pointer pointing to the last character of the string.

- **Line 15:** `bool isPalindrome = true;`  
  A boolean flag assumed to be `true` at the start.

- **Line 17:** `while (left < right) {`  
  Loop runs until both pointers meet in the middle.

- **Line 18-21:**  
  ```cpp
  if (str[left] != str[right]) {
      isPalindrome = false;
      break;
  }
  ```  
  Compares character at `left` with character at `right`. If they do not match, the string is definitely not a palindrome; sets `isPalindrome = false` and terminates the loop immediately.

- **Line 22-23:**  
  ```cpp
  left++;
  right--;
  ```  
  Moves `left` pointer one step right, and `right` pointer one step left towards the center.

- **Line 24:** `}`  
  Closes `while` loop.

- **Line 26-30:**  
  Checks `isPalindrome` flag:
  - If `true`, prints `... is a palindrome.`
  - If `false`, prints `... is not a palindrome.`

- **Line 32:** `return 0;`

---

## Example Walkthrough
- Input: `radar`
  - `left = 0 ('r')`, `right = 4 ('r')` $\rightarrow$ Match! `left=1, right=3`
  - `left = 1 ('a')`, `right = 3 ('a')` $\rightarrow$ Match! `left=2, right=2`
  - Loop finishes (`left < right` becomes false)
  - Output: `radar is a palindrome.`
- Input: `hello`
  - `left = 0 ('h')`, `right = 4 ('o')` $\rightarrow$ Not equal $\rightarrow$ `isPalindrome = false`
  - Output: `hello is not a palindrome.`
