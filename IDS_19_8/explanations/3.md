# Question 3: Print Highest and Lowest Element from 'n' Elements

## Problem Statement
Write a program to print highest and lowest element from n number of elements.

---

## Complete Code (`3.cpp`)
```cpp
// 3. write a program to print highest and low element from n number of elements

#include <iostream>
using namespace std;

int main() {
    int n;
    cout << "Enter number of elements: ";
    cin >> n;

    int arr[n];
    cout << "Enter " << n << " elements:" << endl;
    for (int i = 0; i < n; i++) {
        cin >> arr[i];
    }

    int high = arr[0];
    int low = arr[0];

    for (int i = 1; i < n; i++) {
        if (arr[i] > high) {
            high = arr[i];
        }
        if (arr[i] < low) {
            low = arr[i];
        }
    }

    cout << "highest element: " << high << endl;
    cout << "lowest element: " << low << endl;

    return 0;
}
```

---

## Line-by-Line Explanation

- **Line 1:** `// 3. write a program to print highest and low element from n number of elements`  
  Title comment for the program.

- **Line 3:** `#include <iostream>`  
  Enables standard input and output streams (`cin` and `cout`).

- **Line 4:** `using namespace std;`  
  Brings standard library elements into the default namespace.

- **Line 6:** `int main() {`  
  Entry point of execution.

- **Line 7:** `int n;`  
  Variable to store how many numbers will be entered.

- **Line 8:** `cout << "Enter number of elements: ";`  
  Prompts user for array length.

- **Line 9:** `cin >> n;`  
  Stores user input into variable `n`.

- **Line 11:** `int arr[n];`  
  Declares an array of size `n`.

- **Line 12:** `cout << "Enter " << n << " elements:" << endl;`  
  Prompts user for the elements.

- **Line 13-15:**  
  ```cpp
  for (int i = 0; i < n; i++) {
      cin >> arr[i];
  }
  ```  
  Loops $n$ times to read each element into the array.

- **Line 17:** `int high = arr[0];`  
  Initializes `high` with the first element, assuming it is currently the largest.

- **Line 18:** `int low = arr[0];`  
  Initializes `low` with the first element, assuming it is currently the smallest.

- **Line 20:** `for (int i = 1; i < n; i++) {`  
  Iterates from index 1 to $n - 1$ to check each remaining element.

- **Line 21-23:**  
  ```cpp
  if (arr[i] > high) {
      high = arr[i];
  }
  ```  
  If the current element is greater than `high`, update `high`.

- **Line 24-26:**  
  ```cpp
  if (arr[i] < low) {
      low = arr[i];
  }
  ```  
  If the current element is smaller than `low`, update `low`.

- **Line 27:** `}`  
  Closes the comparison loop.

- **Line 29:** `cout << "highest element: " << high << endl;`  
  Prints the maximum value found.

- **Line 30:** `cout << "lowest element: " << low << endl;`  
  Prints the minimum value found.

- **Line 32:** `return 0;`  
  Successful program termination.

- **Line 33:** `}`  
  Closes `main()`.

---

## Example Walkthrough
- **Input:**  
  `n = 4`  
  `Elements: 12 95 3 45`
- Start: `high = 12`, `low = 12`
- Check 95: $95 > 12 \rightarrow$ `high = 95`
- Check 3: $3 < 12 \rightarrow$ `low = 3`
- Check 45: no change
- **Output:**  
  `highest element: 95`  
  `lowest element: 3`
