# Question 5: Grade Point and Credit Point for 28 Students

## Problem Statement
Write a program to find grade point and credit point occurred for all the 28 students.

---

## Complete Code (`5.cpp`)
```cpp
// 5. write a program to find grade point and credit point for all the 28 students

#include <iostream>
using namespace std;

int main() {
    int n = 28;
    float gradePoints[28];
    int credits[28];
    float totalPoints[28];

    cout << "Enter details for 28 students:" << endl;

    for (int i = 0; i < n; i++) {
        cout << "\nStudent " << i + 1 << ":" << endl;
        cout << "Enter Grade Point (e.g. 8.5): ";
        cin >> gradePoints[i];
        cout << "Enter Credit: ";
        cin >> credits[i];

        totalPoints[i] = gradePoints[i] * credits[i];
    }

    cout << "\n--- Student Grade & Credit Details ---" << endl;
    cout << "Roll No\tGrade Point\tCredits\tTotal Points" << endl;
    for (int i = 0; i < n; i++) {
        cout << i + 1 << "\t" << gradePoints[i] << "\t\t" << credits[i] << "\t" << totalPoints[i] << endl;
    }

    return 0;
}
```

---

## Line-by-Line Explanation

- **Line 1:** `// 5. write a program to find grade point and credit point for all the 28 students`  
  Title comment.

- **Line 3:** `#include <iostream>`  
  Standard I/O stream library for terminal communication.

- **Line 4:** `using namespace std;`  
  Uses standard namespace elements directly.

- **Line 6:** `int main() {`  
  Main entry point.

- **Line 7:** `int n = 28;`  
  Defines the fixed count of students as 28.

- **Line 8:** `float gradePoints[28];`  
  Declares a floating-point array of size 28 to store grade points (e.g., 8.5, 9.0).

- **Line 9:** `int credits[28];`  
  Declares an integer array of size 28 to store credits associated with each student's course.

- **Line 10:** `float totalPoints[28];`  
  Declares an array to store the product (Grade Point $\times$ Credit).

- **Line 12:** `cout << "Enter details for 28 students:" << endl;`  
  Prints introductory prompt.

- **Line 14:** `for (int i = 0; i < n; i++) {`  
  Loops 28 times (from $i = 0$ to $i = 27$) to get data for each student.

- **Line 15:** `cout << "\nStudent " << i + 1 << ":" << endl;`  
  Prints student index starting from 1 (1 to 28).

- **Line 16-17:**  
  ```cpp
  cout << "Enter Grade Point (e.g. 8.5): ";
  cin >> gradePoints[i];
  ```  
  Prompts and inputs the grade point for the $i$-th student.

- **Line 18-19:**  
  ```cpp
  cout << "Enter Credit: ";
  cin >> credits[i];
  ```  
  Prompts and inputs the credit value for the $i$-th student.

- **Line 21:** `totalPoints[i] = gradePoints[i] * credits[i];`  
  Calculates earned credit points by multiplying grade point by credit count.

- **Line 22:** `}`  
  Ends data input loop.

- **Line 24-25:**  
  ```cpp
  cout << "\n--- Student Grade & Credit Details ---" << endl;
  cout << "Roll No\tGrade Point\tCredits\tTotal Points" << endl;
  ```  
  Prints a clean tabular header using `\t` (tab spaces).

- **Line 26-28:**  
  ```cpp
  for (int i = 0; i < n; i++) {
      cout << i + 1 << "\t" << gradePoints[i] << "\t\t" << credits[i] << "\t" << totalPoints[i] << endl;
  }
  ```  
  Displays the tabulated results for each of the 28 students.

- **Line 30:** `return 0;`  
  Normal exit.

- **Line 31:** `}`  
  Closes `main()`.

---

## Example Walkthrough
- For Student 1: Grade Point = 8.5, Credit = 4
  - `Total Points = 8.5 * 4 = 34.0`
- Table output:  
  `1    8.5    4    34.0`
