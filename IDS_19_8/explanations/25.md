# Question 25: Selection Sort

## Problem Statement
Implement selection sort.

---

## Complete Code (`25.cpp`)
```cpp
// 25. implement selection sort

#include <iostream>
using namespace std;

int main() {
    int n;

    cout << "Enter number of elements: ";
    cin >> n;

    int arr[n];
    cout << "Enter " << n << " elements:" << endl;
    for (int i = 0; i < n; i++) {
        cin >> arr[i];
    }

    // Selection sort
    for (int i = 0; i < n - 1; i++) {
        int minIndex = i;
        for (int j = i + 1; j < n; j++) {
            if (arr[j] < arr[minIndex]) {
                minIndex = j;
            }
        }
        // Swap the minimum element with arr[i]
        int temp = arr[i];
        arr[i] = arr[minIndex];
        arr[minIndex] = temp;
    }

    cout << "\nSorted array (Selection Sort):" << endl;
    for (int i = 0; i < n; i++) {
        cout << arr[i] << " ";
    }
    cout << endl;

    return 0;
}
```

---

## Line-by-Line Explanation

- **Line 1:** Program objective comment.

- **Line 3-4:** `#include <iostream>` and `using namespace std;`

- **Line 6:** `int main() {`  
  Execution begins.

- **Line 7:** `int n;`  
  Stores the number of elements.

- **Line 9-15:**  
  Prompts and takes $n$ integers into `arr`.

- **Line 18:** `// Selection sort`  
  Comment indicating the sorting section.

- **Line 19:** `for (int i = 0; i < n - 1; i++) {`  
  Outer loop that advances the boundary between the sorted subarray (left) and unsorted subarray (right). Runs from index 0 to $n - 2$.

- **Line 20:** `int minIndex = i;`  
  Assumes that the smallest element in the unsorted portion is at the starting position `i`.

- **Line 21-25:**  
  ```cpp
  for (int j = i + 1; j < n; j++) {
      if (arr[j] < arr[minIndex]) {
          minIndex = j;
      }
  }
  ```  
  Scans the rest of the array (from $i + 1$ to $n - 1$) to find the true minimum element. Whenever an element smaller than `arr[minIndex]` is found, updates `minIndex = j`.

- **Line 26-30:**  
  ```cpp
  // Swap the minimum element with arr[i]
  int temp = arr[i];
  arr[i] = arr[minIndex];
  arr[minIndex] = temp;
  ```  
  Swaps the smallest found element (`arr[minIndex]`) with the element at position `i` using `temp`. Now, index `i` holds the correct sorted value.

- **Line 32-36:**  
  Iterates and prints the final sorted array.

- **Line 38:** `return 0;`

---

## Example Walkthrough
- Input array: `[64, 25, 12, 22, 11]` (`n = 5`)
- **Pass 1 ($i=0$):**
  - Minimum in `[64, 25, 12, 22, 11]` is `11` at index 4.
  - Swap `arr[0]` (64) with `arr[4]` (11) $\rightarrow [11, 25, 12, 22, 64]$
- **Pass 2 ($i=1$):**
  - Minimum in `[25, 12, 22, 64]` is `12` at index 2.
  - Swap `arr[1]` (25) with `arr[2]` (12) $\rightarrow [11, 12, 25, 22, 64]$
- **Pass 3 ($i=2$):**
  - Minimum in `[25, 22, 64]` is `22` at index 3.
  - Swap `arr[2]` (25) with `arr[3]` (22) $\rightarrow [11, 12, 22, 25, 64]$
- **Pass 4 ($i=3$):**
  - Minimum in `[25, 64]` is `25` at index 3 (already in place).
- Sorted Result: `11 12 22 25 64`
