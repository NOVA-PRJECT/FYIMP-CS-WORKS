# Question 2: Find Minimum of Set of 'n' Elements

## Problem Statement
Write a program to find the minimum of a set of 'n' elements.

---

## Complete Code (`2.cpp`)
```cpp
// 2. write a program to find the minimum of set of 'n' elements

#include <iostream>
using namespace std;

int main() {
    int n;
    cout << "Enter number of elements: ";
    cin >> n;

    int arr[n];
    cout << "Enter " << n << " elements:" << endl;
    for (int i = 0; i < n; i++) {
        cin >> arr[i];
    }

    int min = arr[0];
    for (int i = 1; i < n; i++) {
        if (arr[i] < min) {
            min = arr[i];
        }
    }

    cout << "Minimum element is: " << min << endl;

    return 0;
}
```

---

## Line-by-Line Explanation

- **Line 1:** `// 2. write a program to find the minimum of set of 'n' elements`  
  Single-line comment explaining the question.

- **Line 3:** `#include <iostream>`  
  Imports the input/output header for `cin` and `cout`.

- **Line 4:** `using namespace std;`  
  Brings the C++ standard library names into the global scope.

- **Line 6:** `int main() {`  
  Main function header where the program begins executing.

- **Line 7:** `int n;`  
  Declares an integer variable `n` to store the count of elements the user wants to input.

- **Line 8:** `cout << "Enter number of elements: ";`  
  Displays a prompt asking user for the size $n$.

- **Line 9:** `cin >> n;`  
  Takes the integer input from the user and stores it into `n`.

- **Line 11:** `int arr[n];`  
  Declares an array `arr` of size `n` (Variable Length Array) to store the user's $n$ numbers.

- **Line 12:** `cout << "Enter " << n << " elements:" << endl;`  
  Prompts the user to enter the $n$ elements.

- **Line 13:** `for (int i = 0; i < n; i++) {`  
  Loop that runs from index 0 to $n - 1$.

- **Line 14:** `cin >> arr[i];`  
  Reads each number into `arr[i]`.

- **Line 15:** `}`  
  Closes the input loop.

- **Line 17:** `int min = arr[0];`  
  Initializes `min` with the first element `arr[0]`.

- **Line 18:** `for (int i = 1; i < n; i++) {`  
  Loops through the remaining elements from index 1 to $n - 1$.

- **Line 19:** `if (arr[i] < min) {`  
  Checks if current element is smaller than the current `min`.

- **Line 20:** `min = arr[i];`  
  If true, updates `min` with `arr[i]`.

- **Line 21:** `}`  
  Closes the `if` statement.

- **Line 22:** `}`  
  Closes the comparison loop.

- **Line 24:** `cout << "Minimum element is: " << min << endl;`  
  Prints the smallest number found in the array.

- **Line 26:** `return 0;`  
  Terminates program cleanly.

- **Line 27:** `}`  
  Closes `main()` function.

---

## Example Walkthrough
- **Input:**  
  `Enter number of elements: 5`  
  `Enter 5 elements:`  
  `30 15 70 8 42`
- Loop iterates and compares:
  - 15 < 30 $\rightarrow$ `min = 15`
  - 70 not < 15
  - 8 < 15 $\rightarrow$ `min = 8`
  - 42 not < 8
- **Output:** `Minimum element is: 8`
